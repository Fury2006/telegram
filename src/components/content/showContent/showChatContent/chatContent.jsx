import { Route, Routes } from "react-router-dom";

const chatContent = (prosp) => {
    return ( 
        <Routes>
            <Route path='/Sergiy' />
        </Routes>
     );
}
 
export default chatContent;